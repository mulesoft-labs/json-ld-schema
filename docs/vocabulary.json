{
    "$schema": "https://json-schema.org/draft/2019-09/schema",
    "$id": "https://a.ml/schemas/semantics/2019-10",
    "$vocabulary": {
		"https://a.ml/schemas/semantics/2019-10": true
	},
    "$recursiveAnchor": true,
    "title": "Semantics annotations for a JSON-Schema",
    "properties": {
		"@context": {
			"description": "Used to define the short-hand names that are used throughout a JSON-LD document.",
			"type": ["object", "string", "array", "null"]
		},
		"@type": {
			"description": "Used to set the data type of a node or typed value.",
			"type": ["string", "null", "array"]
		},
		"additionalProperties": { "$recursiveRef": "#" }
	},
    "additionalProperties": true
}
